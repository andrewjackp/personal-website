// inputs
// p (principal)
// r (rate of interest)
// t (number of years; amount is invested)
// n (number of times the is compounded per year)
// A (ending amount)

// output ---> A

// processses
// A = p * (1 + r/n) ^ n * t

// constraints
// value of r will have to divided by 100

// fractions of a cent rounded up to the next penny
// format as money

// prompt for all inputs
// principal > 0;
// set min/max of principal

// define p
// -- type:number
// set min/max

// define r
// -- type:number, divided by 100
// set min/max

// define t
// -- type: number
// set min/max

// define n
// -- type: number
// set min/max

// define A
// result of all inputs and processes

// areInputsValid ()
//		var inputs = document.querySelectorAll('inputs')
//		inputs.forEach (input => {
//			if (input.value) {
//				if (input.value > 0) {
//					return true;	
// }			else return false;


// if(areInputsValid) = true
// run calcCompInterest


// calcCompInterest(p, r, t, n){
// 	A = p * (1 + r/n) ^ n * t
//	return A
// }

// assign r/n to a variable
// assign n*t to a variable 